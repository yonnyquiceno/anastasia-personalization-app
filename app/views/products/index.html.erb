<h4>Products</h4>

<table class="table">

  <thead>
    <tr>
      <th>Comprador</th>
      <th>Fabricante</th>
      <th>Categoría</th>
      <th>Referencia</th>
      <th>Aprobado</th>
      <th>Enviado</th>
      <th>Precio</th>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |m| %>
      <tr>
        <td><%= m.user_id %></td>
        <td><%= m.manufacturer_id %></td>
        <td><%= m.category.category_name%></td>
        <td><%= m.article.article_name %></td>
        <td><%= m.approved %></td>
        <td><%= m.sended %></td>
        <td><%= m.price %></td>
      </tr>
    <% end %>
  </tbody>

</table>



<%= link_to "Inicio", root_path %>
